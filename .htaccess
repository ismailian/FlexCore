# BEGIN FlexCore
<IfModule mod_rewrite.c>

    RewriteEngine On
    # Options -Indexes
    # RewriteBase /Core/

    # Rewrite Conditions #
    # RewriteCond %{REQUEST_FILENAME} !-f
    # RewriteCond %{REQUEST_FILENAME} !-d

    # Assets Folder #
    # RewriteRule ^assets\/(.+)$ Views/assets/$1 [L]

    # Prevent Access to app.json
    RewriteRule ^/?Config/app\.json$ Core/App.php [QSA,L]

    # Rewrite Rules for Views Templates #
    
    #RewriteRule ^ Core/ [QSA,L]


</IfModule>
# END FlexCore
