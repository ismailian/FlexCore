<?php 


class Token
{

	/**
	* This class creates, refreshes and deletes tokens.
	*/


	/** 
	* @description Make is a method for generating token with a specific string length (def: 255).
	* @param int $length Length pertains to the length of token to be generated.
	* @return string return a string token with the given length.  
	*/
	public static function make($length = 255)
	{
		// code
	}
}
